import React, { FC, ReactElement } from 'react';
import { NormalInput } from './input.styles';

const Input: FC = (): ReactElement => <NormalInput placeholder="Your email address" />;

export default Input;
